import { VillagerFilterPipe } from './villager-filter.pipe';

describe('VillagerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VillagerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
